import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import PresupuestoOperativo from './PresupuestoOperativo';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <PresupuestoOperativo />
  </React.StrictMode>
);
